# =============================================================================
# RESONANCEDB PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in the required values
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - Application will not start without these
# -----------------------------------------------------------------------------

# Domain configuration
DOMAIN=resonancedb.example.com
ACME_EMAIL=admin@example.com

# Database credentials
POSTGRES_USER=resonance_prod
POSTGRES_PASSWORD=<generate-strong-password>

# API Security
# Generate with: openssl rand -hex 32
API_KEY_SECRET=<generate-with-openssl-rand-hex-32>

# -----------------------------------------------------------------------------
# OPTIONAL - Have sensible defaults
# -----------------------------------------------------------------------------

# Traefik Dashboard Auth (generate with: htpasswd -nb admin password)
# Leave empty to disable dashboard
TRAEFIK_DASHBOARD_AUTH=

# CORS Origins (JSON array format)
# Default is set in docker-compose.prod.yml based on DOMAIN
# CORS_ORIGINS=["https://resonancedb.example.com"]

# Redis URL (defaults to internal redis container)
# REDIS_URL=redis://redis:6379

# S3 for data archival (optional)
# S3_BUCKET=resonancedb-samples
# S3_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# -----------------------------------------------------------------------------
# DEPLOYMENT COMMANDS
# -----------------------------------------------------------------------------
#
# First deployment:
#   docker-compose -f docker-compose.prod.yml up -d
#
# Update after code changes:
#   docker-compose -f docker-compose.prod.yml build
#   docker-compose -f docker-compose.prod.yml up -d
#
# View logs:
#   docker-compose -f docker-compose.prod.yml logs -f
#
# Backup database:
#   docker exec resonancedb-postgres pg_dump -U $POSTGRES_USER resonancedb > backup.sql
#
# =============================================================================
